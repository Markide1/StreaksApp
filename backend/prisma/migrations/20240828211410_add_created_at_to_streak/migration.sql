BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Streak] ADD [createdAt] DATETIME2 NOT NULL CONSTRAINT [Streak_createdAt_df] DEFAULT CURRENT_TIMESTAMP,
[deletedAt] DATETIME2;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
